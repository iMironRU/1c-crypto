# Claude.md — Инструкции для ИИ-ассистента

## Что это за проект

Мини-курс «Криптография в 1С» — учебный материал для разработчиков 1С,
которые знают язык, но никогда не работали с криптографией.

Сопровождается демонстрационной конфигурацией 1С с рабочими примерами кода,
которые читатель может запустить и использовать в своих проектах.

## Аудитория

Разработчик 1С с опытом от 1 года. Знает язык, работал с документами,
справочниками, запросами. Криптография — новая тема. Слова «хеш», «сертификат»,
«ключ» слышал, но не понимает как это работает и зачем нужно.

## Структура курса

8 уроков, каждый рассчитан на 45 минут.

| Урок | Тема | Файл |
|------|------|------|
| 01 | Зачем это нужно разработчику 1С | `course/lesson-01/` |
| 02 | Хеширование: необратимый отпечаток | `course/lesson-02/` |
| 03 | Хранение паролей: почему не шифрование | `course/lesson-03/` |
| 04 | Ключи, сертификаты, криптопровайдеры | `course/lesson-04/` |
| 05 | Шифрование: сейф для данных | `course/lesson-05/` |
| 06 | Электронная подпись: цифровая печать | `course/lesson-06/` |
| 07 | Криптография в реальных задачах | `course/lesson-07/` |
| 08 | Диагностика: когда что-то пошло не так | `course/lesson-08/` |

## Структура каждого урока

Каждый урок — папка `course/lesson-NN/` с тремя файлами:

```
lesson-NN/
├── README.md       # Основной текст урока
├── questions.md    # Контрольные вопросы (5 штук)
└── answers.md      # Ответы на контрольные вопросы
```

### Шаблон README.md урока

```
# Урок N. Название

> Время: ~45 минут
> Что нужно до начала: [prerequisites]
> Что будет после: [outcome]

## Зачем это знать

[Аналогия из жизни, без кода, 2–3 абзаца]

## Как это работает

[Теория, минимально необходимая, с аналогиями]

## Пример [название]

[Пошаговый разбор]

```1c
// Код
```

[Объяснение что происходит в коде]

## Что запомнить

- Пункт 1
- Пункт 2
- Пункт 3

## Контрольные вопросы

→ [questions.md](questions.md)
```

## Фундаментальный принцип: всё — двоичные данные

Это самое важное, что читатель должен понять до первого примера.

**Криптография работает только с байтами.** Не со строками, не с числами,
не с датами — только с последовательностями байт (`ДвоичныеДанные`).
Любой текст перед криптографической операцией превращается в байты.
Результат любой операции — тоже байты.

Это означает два обязательных шага в каждом примере:

```
Строка → ДвоичныеДанные  →  [крипто-операция]  →  ДвоичныеДанные → Строка/Хранилище
```

**Правило для всех уроков и примеров кода:**
- Каждый пример, где на вход идёт текст, явно показывает преобразование
  строки в `ДвоичныеДанные` перед операцией.
- Каждый пример, где результат — двоичные данные, явно показывает
  как их представить (Base64, HEX) и как сохранить в базе.
- Нельзя «прятать» это внутри функции без объяснения — читатель
  должен видеть и понимать каждый шаг.

Подробно: `docs/binary-data.md`

## Правила написания текстов

**Тон:** дружелюбный, прямой. Не «рекомендуется применять», а «используйте».
Не «следует отметить», а «важно».

**Аналогии:** каждую новую концепцию вводить через бытовую аналогию
*до* технического объяснения. Аналогия — не украшение, а точка входа.

**Код:** каждый пример должен быть воспроизводимым. Если для запуска
нужны предварительные шаги (установить сертификат, создать файл) —
описать их явно, пошагово, прямо перед кодом.

**Примечания ГОСТ:** когда пример работает через Microsoft CSP,
добавлять блок «В боевой системе» с указанием что именно меняется
при переходе на КриптоПро. Одно место, одна строка.

**Длина раздела:** ~300–500 слов на смысловой блок. Если больше —
делить на подразделы.

## Правила написания кода

**Язык:** встроенный язык 1С (BSL), русские ключевые слова.

**Комментарии:** каждая нетривиальная строка — с комментарием.
Код должен читаться как псевдокод.

**Переменные:** имена на русском, описательные.
`ХешДвоичные` а не `х` или `result`.

**Обработка ошибок:** все примеры с `Попытка/Исключение`,
с понятным сообщением об ошибке.

**Файлы кода:** расширение `.bsl`. Имя файла — kebab-case,
описывает что делает код: `hash-password-with-salt.bsl`.

**Воспроизводимость:** в начале каждого файла кода —
блок комментариев с требованиями:

```
// Требования:
// - Платформа 1С 8.3.19+
// - Windows (Microsoft Strong Cryptographic Provider встроен в ОС)
// - Для ГОСТ: КриптоПро CSP 5.x
```

## Структура кода (папка /code)

```
code/
├── hashing/
│   ├── hash-string.bsl              # Хеш строки (SHA256)
│   ├── hash-file.bsl                # Хеш файла
│   ├── hash-password-with-salt.bsl  # Хранение пароля
│   └── verify-password.bsl          # Проверка пароля
├── encryption/
│   ├── encrypt-string.bsl           # Шифрование строки
│   ├── decrypt-string.bsl           # Расшифровка строки
│   └── encrypt-file.bsl             # Шифрование файла
├── signature/
│   ├── sign-file.bsl                # Подписание файла
│   ├── verify-signature.bsl         # Проверка подписи
│   └── sign-pdf.bsl                 # Подписание PDF (PAdES)
├── https/
│   ├── https-request.bsl            # HTTPS-запрос
│   └── mtls-request.bsl             # mTLS с клиентским сертификатом
├── jwt/
│   └── create-jwt-hs256.bsl         # Создание JWT (HS256)
└── utils/
    ├── binary-to-hex.bsl            # ДвоичныеДанные → HEX
    ├── string-to-binary.bsl         # Строка → ДвоичныеДанные (с кодировкой)
    ├── binary-to-base64.bsl         # ДвоичныеДанные → Base64 и обратно
    └── get-certificate.bsl          # Получение сертификата по отпечатку
```

## Структура конфигурации (папка /configuration)

Конфигурация — учебная, не боевая. Содержит:
- Общие модули с реюзабельными функциями (одноимённые с папками /code)
- Обработки-демо (по одной на урок, от 2 до 8)
- Никаких объектов метаданных кроме общих модулей и обработок

```
configuration/
├── README.md          # Как установить и запустить
├── src/               # Исходники в формате EDT (XML)
│   ├── CommonModules/
│   │   ├── КриптоДвоичныеДанные/    ← конвертации, НовыйРезультат()
│   │   ├── КриптоПровайдер/         ← МенеджерMS(), МенеджерКриптоПро()
│   │   ├── КриптоХеширование/
│   │   ├── КриптоШифрование/
│   │   ├── КриптоПодпись/
│   │   └── КриптоHTTPS/
│   └── DataProcessors/
│       ├── Урок02_Хеширование/
│       ├── Урок03_Пароли/
│       ├── Урок05_Шифрование/
│       ├── Урок06_Подпись/
│       └── Урок07_РеальныеЗадачи/
└── releases/
    └── crypto-1c-demo.cf  # Скомпилированная конфигурация
```

## Прогресс

| Артефакт | Статус |
|----------|--------|
| Claude.md | ✅ Готово |
| README.md (проект) | ✅ Готово |
| Структура репозитория | ✅ Готово |
| docs/binary-data.md | ✅ Готово |
| docs/glossary.md | ✅ Готово |
| docs/certificates.md | ✅ Готово |
| docs/providers.md | ✅ Готово |
| Урок 01 | ✅ Готово |
| Урок 02 | ✅ Готово |
| Урок 03 | ✅ Готово |
| Урок 04 | ✅ Готово |
| Урок 05 | ✅ Готово |
| Урок 06 | ✅ Готово |
| Урок 07 | ✅ Готово |
| Урок 08 | ✅ Готово |
| scripts/Create-1C-Certificate.ps1 | ✅ Готово |
| Код: utils | ✅ Готово |
| Код: hashing | ✅ Готово |
| Код: encryption | ✅ Готово |
| Код: signature | ✅ Готово |
| Код: https | ✅ Готово |
| Код: jwt | ✅ Готово |
| Конфигурация: общие модули (6 шт.) | ✅ Готово |
| Конфигурация: обработки-демо | 🔲 Не начато |
| Конфигурация: .cf файл | 🔲 Не начато |

## Важные решения

- **Двоичные данные — явно.** Каждый пример показывает полный путь:
  строка → `ДвоичныеДанные` → операция → результат → хранение в БД.
  Конвертации не прячутся внутри функций без объяснения.
- **Без платных компонент.** Все примеры работают на встроенном
  Microsoft Strong Cryptographic Provider (Windows).
- **ГОСТ — отдельно.** Переход на КриптоПро описывается как
  отдельный шаг, не перемешивается с основными примерами.
- **Конфигурация: шесть модулей.** КриптоДвоичныеДанные (без зависимостей) →
  КриптоПровайдер → КриптоХеширование / КриптоШифрование / КриптоПодпись / КриптоHTTPS.
- **Возврат результата.** Все публичные функции модулей возвращают `Структура {Успех, Результат, Ошибка}`.
  Создаётся через `КриптоДвоичныеДанные.НовыйРезультат()`.
- **Два трека: без БСП и с БСП.** Основной — без БСП, максимально универсальный.
  БСП-трек (`КриптоБСП` модуль) — отложен, не приоритет.
- **Код = уроки.** Файлы в `/code` — самодостаточные скрипты: определение функции
  + пример использования. Можно копировать в консоль 1С как есть.
